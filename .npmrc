# Better Auth pulls in framework-adapter packages (e.g. @tanstack/react-start) as
# *optional* peer deps that pin a newer Vite than our Vitest toolchain uses. We
# don't use those adapters, so relax peer resolution to avoid a false conflict.
legacy-peer-deps=true
